// import { MEALS } from ../

const initialState = {
  restaurants: [
    {
      id: 1,
      name: 'MC'
    },
    {
      id: 2,
      name: 'Burger King'
    }
  ]
}

const mealsReducer = (state = initialState, action) => {

  return state
}

export default mealsReducer;