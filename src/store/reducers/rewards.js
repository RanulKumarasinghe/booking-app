import { FETCH_REWARD_POINTS } from '../actions/rewards';

const initialState ={
    // points: points
};

const rewardsReducer = (state = initialState, action) => {
    switch (action.type){
        default: 
            return state;
    }
}